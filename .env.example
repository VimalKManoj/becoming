# The foundation runs locally without credentials. Do not copy portfolio secrets.
# Phase 2: run npm run backend and let the Convex CLI write deployment values.
# CONVEX_DEPLOYMENT=
# NEXT_PUBLIC_CONVEX_URL=
# Authentication provider is selected and connected in phase 2.
# Set AUTH_ISSUER_DOMAIN in the Convex dashboard, not as a public frontend variable.
